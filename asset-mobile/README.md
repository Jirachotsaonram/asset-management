# Asset Management Mobile App

แอปพลิเคชันมือถือสำหรับระบบจัดการครุภัณฑ์ (React Native with Expo)

## ฟีเจอร์

- 🔐 **Authentication**: ระบบล็อกอิน
- 📊 **Dashboard**: หน้าจอดashboard แสดงสถิติครุภัณฑ์
- 📷 **QR Code Scanner**: สแกน QR Code เพื่อค้นหาและตรวจสอบครุภัณฑ์
- 📦 **Asset Management**: ดูรายการครุภัณฑ์ทั้งหมด
- ✅ **Check Assets**: ดูรายการครุภัณฑ์ที่ยังไม่ตรวจสอบ
- 🔄 **Borrows**: ดูประวัติการยืม-คืน
- 👤 **Profile**: ข้อมูลผู้ใช้และออกจากระบบ

## ข้อกำหนดเบื้องต้น

- Node.js (v14 หรือสูงกว่า)
- npm หรือ yarn
- Expo CLI
- Android Studio (สำหรับ Android) หรือ Xcode (สำหรับ iOS)

## การติดตั้ง

1. ติดตั้ง dependencies:
```bash
npm install
```

หรือ

```bash
yarn install
```

2. ตั้งค่า API URL:
แก้ไขไฟล์ `src/utils/constants.js` และเปลี่ยน `API_BASE_URL` ให้ชี้ไปที่ API server ของคุณ

**สำคัญ**: สำหรับการทดสอบบนอุปกรณ์จริงหรือ emulator ต้องใช้ IP address ของเครื่องที่รัน API แทน `localhost`

ตัวอย่าง:
```javascript
export const API_BASE_URL = 'http://192.168.1.100/asset-management/asset_management_api';
```

## การรันแอป

### Development Mode
```bash
npm start
```

### Android
```bash
npm run android
```

### iOS
```bash
npm run ios
```

### Web
```bash
npm run web
```

## โครงสร้างโปรเจกต์

```
asset-mobile/
├── src/
│   ├── components/       # Components ที่ใช้ร่วมกัน
│   ├── contexts/         # React Contexts (Auth, etc.)
│   ├── hooks/            # Custom Hooks
│   ├── screens/          # หน้าจอต่างๆ
│   ├── services/         # API Services
│   └── utils/            # Utilities และ Constants
├── App.js                # Main App Component
├── app.json              # Expo Configuration
└── package.json          # Dependencies
```

## หน้าจอหลัก

1. **LoginScreen**: หน้าล็อกอิน
2. **DashboardScreen**: หน้าหลักแสดงสถิติ
3. **ScanScreen**: สแกน QR Code
4. **AssetsScreen**: รายการครุภัณฑ์
5. **CheckScreen**: รายการที่ต้องตรวจสอบ
6. **BorrowsScreen**: การยืม-คืน
7. **ProfileScreen**: โปรไฟล์ผู้ใช้

## การใช้ QR Code Scanner

1. ไปที่หน้าจอ "สแกน QR"
2. กดปุ่ม "สแกน QR Code" หรือกรอกรหัสครุภัณฑ์
3. อนุญาตการใช้งานกล้อง
4. สแกน QR Code บนครุภัณฑ์
5. ตรวจสอบข้อมูลและบันทึกการตรวจสอบ

## หมายเหตุ

- แอปนี้ใช้ Expo ซึ่งช่วยให้การพัฒนาและทดสอบง่ายขึ้น
- สำหรับ production build ควรใช้ `expo build` หรือ EAS Build
- ตรวจสอบให้แน่ใจว่า API server รองรับ CORS และอนุญาตการเข้าถึงจาก mobile app

## การแก้ไขปัญหา

### ไม่สามารถเชื่อมต่อกับ API ได้
- ตรวจสอบว่า API_BASE_URL ถูกต้อง
- ตรวจสอบว่า API server กำลังทำงานอยู่
- สำหรับ Android emulator ใช้ `10.0.2.2` แทน `localhost`
- สำหรับ iOS simulator ใช้ `localhost` ได้
- สำหรับอุปกรณ์จริง ใช้ IP address ของเครื่องที่รัน API

### ไม่สามารถสแกน QR Code ได้
- ตรวจสอบว่าอนุญาตการใช้งานกล้องแล้ว
- ตรวจสอบว่าแอปมีสิทธิ์ในการเข้าถึงกล้อง (ตรวจสอบใน Settings)

## License

MIT

