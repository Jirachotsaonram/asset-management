# แผนภาพกระแสข้อมูลระดับ 0 (Data Flow Diagram - DFD Level 0)

แผนภาพ **DFD Level 0** (หรือ Diagram 0) แสดงกระบวนการหลักของระบบ (Main Processes) ทั้งหมด 6 กระบวนการ พร้อมทั้งแสดงความสัมพันธ์และการไหลของข้อมูลระหว่างหน่วยงานภายนอก (External Entities) และแหล่งเก็บข้อมูล (Data Stores)

## 📊 DFD Level 0: ระบบบริหารจัดการครุภัณฑ์

```mermaid
graph TD
    classDef extEntity fill:#f3f4f6,stroke:#4b5563,stroke-width:2px;
    classDef process fill:#ffffff,stroke:#0f172a,stroke-width:2px,rx:15,ry:15;
    classDef datastore fill:#ffffff,stroke:#0f172a,stroke-width:2px;

    %% External Entities (หน่วยงานภายนอก)
    Admin["ผู้ดูแลระบบ (Admin)"]:::extEntity
    Inspector["เจ้าหน้าที่ (Inspector)"]:::extEntity
    Viewer["ผู้ใช้งานทั่วไป (Viewer)"]:::extEntity

    %% Processes (กระบวนการหลัก)
    P1("1.0<br/>จัดการบัญชีและสิทธิ์"):::process
    P2("2.0<br/>จัดการข้อมูลพื้นฐาน"):::process
    P3("3.0<br/>จัดการข้อมูลครุภัณฑ์"):::process
    P4("4.0<br/>การสำรวจและตรวจสภาพ"):::process
    P5("5.0<br/>จัดการยืม-คืน"):::process
    P6("6.0<br/>แดชบอร์ดและรายงาน"):::process

    %% Data Stores (แหล่งเก็บข้อมูล)
    D1[("D1 ข้อมูลผู้ใช้งาน")]:::datastore
    D2[("D2 ข้อมูลโครงสร้างพื้นฐาน")]:::datastore
    D3[("D3 ข้อมูลครุภัณฑ์")]:::datastore
    D4[("D4 ข้อมูลประวัติเคลื่อนย้าย")]:::datastore
    D5[("D5 ข้อมูลการตรวจสภาพ")]:::datastore
    D6[("D6 ข้อมูลการยืม-คืน")]:::datastore
    D7[("D7 ข้อมูล Audit Trail")]:::datastore

    %% --------------------------------
    %% Data Flows: External Entity <--> Process
    %% --------------------------------

    %% Admin Flows
    Admin -- "ข้อมูลบัญชีผู้ใช้" --> P1
    P1 -- "สถานะตรวจสอบสิทธิ์" --> Admin
    
    Admin -- "ข้อมูลแผนก/สถานที่" --> P2
    
    Admin -- "รายละเอียดครุภัณฑ์/ไฟล์ Import" --> P3
    P3 -- "ไฟล์บาร์โค้ด PDF/ผลการนำเข้า" --> Admin
    
    Admin -- "กำหนดรอบการตรวจประจำปี" --> P4
    
    Admin -- "เงื่อนไขการขอรายงาน" --> P6
    P6 -- "รายงานสถิติ และ Log" --> Admin

    %% Inspector Flows
    Inspector -- "สแกน QR Code / ผลประเมินและภาพถ่าย" --> P4
    P4 -- "สถานะและข้อมูลครุภัณฑ์ปัจจุบัน" --> Inspector
    
    Inspector -- "ข้อมูลรายการยืม/คืน" --> P5
    P5 -- "สถานะการทำรายการ" --> Inspector

    %% Viewer Flows
    Viewer -- "คำค้นหา/เงื่อนไข" --> P6
    P6 -- "แดชบอร์ดสรุปสถิติภาพรวม" --> Viewer

    %% --------------------------------
    %% Data Flows: Process <--> Data Store
    %% --------------------------------

    P1 <--> |"บันทึก/ดึงข้อมูลบัญชี"| D1
    
    P2 <--> |"บันทึก/ดึงข้อมูลแผนกสถานที่"| D2
    
    P3 <--> |"บันทึก/ดึงรายละเอียดครุภัณฑ์"| D3
    P3 --> |"บันทึกประวัติการย้าย"| D4
    P3 --> |"บันทึก Log การนำเข้า"| D7
    
    P4 <--> |"บันทึก/ดึงข้อมูลการตรวจ"| D5
    P4 --> |"อัปเดตสถานะล่าสุด (ปกติ/ชำรุด)"| D3
    
    P5 <--> |"บันทึก/ดึงข้อมูลยืม-คืน"| D6
    P5 --> |"อัปเดตสถานะ (ถูกยืม/ว่าง)"| D3
    
    P6 <-- "ดึงข้อมูลสถิติ" --> D1
    P6 <-- "ดึงข้อมูลดิบ" --> D3
    P6 <-- "ดึงข้อมูลการตรวจ" --> D5
    P6 <-- "ดึงข้อมูลยืมคืน" --> D6
    P6 <-- "ดึงข้อมูล Log" --> D7

```

---

## 📝 คำอธิบายทิศทางการไหลของข้อมูล (Data Flow Explanation)

**การปฏิสัมพันธ์กับผู้ดูแลระบบ (Admin):**
* ทำหน้าที่จัดการข้อมูลทุกส่วน ตั้งแต่บัญชีผู้ใช้ (Process 1.0) โครงสร้างพื้นฐาน (Process 2.0) และฐานข้อมูลครุภัณฑ์หลัก (Process 3.0) 
* เมื่อมีอุปกรณ์ใหม่ สามารถส่งข้อมูลแบบฟอร์ม หรือไฟล์ Excel เข้าไปในระบบ และรับไฟล์บาร์โค้ด PDF กลับออกมาแปะที่อุปกรณ์ได้
* ร้องขอและเรียกดูรายงาน (Process 6.0) รวมถึงประวัติการใช้งาน (Audit Trail) เชิงลึกได้

**การปฏิสัมพันธ์กับเจ้าหน้าที่ (Inspector):**
* ใช้งานผ่าน Mobile App เป็นหลัก โดยส่งคำขอตรวจสอบข้อมูลผ่านการสแกน QR Code (Process 4.0) และส่งผลการประเมินสภาพ/รูปถ่ายเข้าไปบันทึกใน Data Store D5
* ส่งข้อมูลคำขอการยืม-คืน (Process 5.0) ของผู้ใช้เข้าไปบันทึกใน Data Store D6 พร้อมเปลี่ยนสถานะครุภัณฑ์ในแฟ้ม D3 แบบอัตโนมัติ

**การปฏิสัมพันธ์กับผู้บริหาร/ผู้ใช้ทั่วไป (Viewer):**
* ไม่มีการแก้ไขข้อมูล (Write) แต่จะทำการส่งเงื่อนไขเพื่อเรียกดูข้อมูล (Read) ผ่านหน้า Dashboard (Process 6.0) เพื่อดูยอดรวมสถิติต่างๆ

**การไหลเวียนภายในกับแหล่งเก็บข้อมูล (Data Stores):**
* **D3 ข้อมูลครุภัณฑ์** ถือเป็นศูนย์กลาง (Hub) ที่กระบวนการ P3, P4 และ P5 ต้องเข้าถึงเพื่ออัปเดตสถานะ (เช่น จาก "ปกติ" เป็น "ชำรุด" หรือ "ถูกยืม")
* กระบวนการ **P6 แดชบอร์ดและรายงาน** จะทำการดึงข้อมูล (Read Only) จาก Data Store ทุกตัว (D1, D3, D5, D6, D7) เพื่อนำมาสรุปผลและออกรายงาน โดยไม่ไปแก้ไขข้อมูลใดๆ
